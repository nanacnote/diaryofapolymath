FROM node:lts-alpine

RUN apk update \
    && apk add git

RUN npm install -g \
    semantic-release \
    @semantic-release/changelog \
    @semantic-release/git

ENTRYPOINT ["./run", "start:release"]
